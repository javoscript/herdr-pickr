## 1. Project Description

- [x] 1.1 Create root-level `AGENTS.md` with a project-description section identifying Pickr as a local Herdr plugin for fuzzy tab, space, and agent pickers with terminal previews, using Lua 5.3+, `luv`, and `fzf`; verify the file exists at the repository root and the description agrees with `README.md`, `herdr-plugin.toml`, and `main.lua`.

## 2. Commit Guideline

- [x] 2.1 Add a commit-guidelines section stating "Commits should be made following the Conventional Commits format." Include `type(scope): description`, explain that scope is optional, and give an example such as `docs: add project agent guidelines`; verify the rendered Markdown clearly presents both the guideline and a correctly formatted example.
