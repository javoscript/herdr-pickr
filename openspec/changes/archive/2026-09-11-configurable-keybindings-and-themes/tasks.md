## 1. Configuration resolution

- [x] 1.1 Add injectable Pickr config-directory discovery using the applicable `HERDR_PLUGIN_CONFIG_DIR` and Herdr's plugin config-dir lookup; verify fixtures for managed, linked, direct, unrelated-plugin environment, custom config location, and paths containing spaces.
- [x] 1.2 Implement optional JSON loading, partial defaults, null-as-omitted behavior distinct from empty arrays, and strict setting/type diagnostics; verify missing-file defaults, null key/theme/color settings, and blocking unreadable-file, malformed JSON, unknown-setting, and wrong-type errors without creating or modifying user config files.
- [x] 1.3 Resolve configuration once in the launcher for action launches and once in the picker owner for direct launches; hand the launcher's validated settings snapshot to the owner and pass it through variant switching and refresh. Verify edits between launcher resolution and picker startup or during a popup have no effect until a subsequent launch, and preview/control helpers do not independently reload settings.
- [x] 1.4 Implement `preview.enabled_by_default` with a true default and omitted/null fallback; reject wrong types and unknown preview settings. Use the configured visibility only to initialize a new popup session or direct picker invocation, and maintain current preview visibility as separate session state. Verify new launches of all five variants start with the configured visibility, configured toggling works from both initial states, and all 25 source/destination routes preserve the current shown/hidden state, including empty lists, zero matches, switches during refresh, and switches after failure. Verify refresh/retry preserve toggled visibility, switching still resets the query and preserves the original workspace, and reopening resets visibility to the configured default and adopts edits. Document the setting and record interactive acceptance for both initial states and toggled-state preservation across switches.
- [x] 1.5 Implement `popup.width` and `popup.height` with 80% × 70% defaults, independent omitted/null fallback, percentage strings, and nonnegative finite integer outer-cell counts capped at Herdr 0.9.0's maximum of 65535 during configuration resolution. Verify oversized cell counts are capped in the launch snapshot and socket request. Reject unknown settings, wrong types, malformed or out-of-range percentages, and negative, non-finite or fractional numbers before launch. Change `src/open.lua` to pass dimensions, original workspace, and the settings snapshot through `plugin.pane.open` socket requests for all five actions; verify request/reply handling, failure/timeout cleanup, direct-launch defaults, unchanged geometry through switching/refresh, and reopen-to-adopt behavior. Document and record interactive acceptance for default, percentage, and cell-count dimensions with Herdr clamping.

## 2. Pickr action keymap

- [x] 2.1 Add the nine action defaults, replacement arrays, effective-key normalization, and validation; verify unchanged defaults, multiple aliases, empty-array support for all seven optional actions, rejection for empty accept/close arrays, unknown keys, event/action-expression rejection, and conflicts including fzf-equivalent key aliases.
- [x] 2.2 Generate fzf bindings, variant expectation keys, reverse routing, and footer/retry hints from the resolved action map; verify all 25 enabled source/destination routes with remapped keys, including empty lists and zero matches, every configured alias in hints, and disabled actions absent from hints in all states. Verify real-fzf acceptance output and closing when all optional shortcuts are disabled, plus external Herdr action availability for disabled variant shortcuts.
- [x] 2.3 Remove remapped default action routes and guard against built-in acceptance/cancellation aliases bypassing configuration; verify real fzf accepts/closes only through the configured action keys and a released key can regain an inherited navigation/editing binding.
- [x] 2.4 Drive refresh unbinding/rebinding from all resolved accept and refresh aliases; verify slow refresh, repeated mixed refresh aliases, failure/retry, no deferred acceptance, and closing/enabled-switching cancellation with remapped keys while preserving query, preview visibility, and highlighted identity. Verify disabled refresh has no shortcut/hint, and disabled preview/variant shortcuts are never restored or advertised during loading, failure, or successful retry.

## 3. General fzf binding inheritance

- [x] 3.1 Characterize fzf 0.74.3 option-file/environment precedence, tokenization, repeated bind handling, aliases, separators, and action chains from its source/docs; deliver a supported navigation/editing/preview-scrolling action inventory and compatibility fixtures checked against real fzf before implementing the importer.
- [x] 3.2 Implement non-shell parsing and import of supported `--bind VALUE` and `--bind=VALUE` settings from both ambient sources; verify quoting, special separator keys, repeated assignments, source precedence, supported chains, unreadable source files, and malformed input diagnostics with deterministic fixtures.
- [x] 3.3 Exclude unsupported/event bindings as whole bindings with non-blocking key/event-and-action diagnostics, isolate unrelated options and `FZF_API_KEY`, and overlay Pickr's effective bindings and events; verify supported navigation/editing/preview scrolling still works, excluded bindings do not prevent launch, conflicts favor Pickr, and ambient output, preview toggling, color, accept/reload/transform, and mixed-chain options cannot alter owner-controlled behavior or revive disabled actions.

## 4. Named themes and role overrides

- [x] 4.1 Bundle the 18 Herdr v0.9.0 canonical theme mappings with source provenance and applicable attribution; verify the full inventory, complete role coverage, Catppuccin default, explicit Rosé Pine compatibility, and ANSI/default treatment of `terminal`.
- [x] 4.2 Implement semantic role override validation and RGB/ANSI/default serialization matching Herdr 0.9.0's valid color formats and aliases; verify short/long hex, `rgb(r,g,b)` component bounds, every documented ANSI name/alias, reset/default/none/transparent equivalence, case/whitespace normalization, blocking invalid role/color errors, and partial/null overrides retaining palette values.
- [x] 4.3 Replace hardcoded fzf, indicator, header, and annotation colors with the shared resolved role table, including refresh rendering; verify all five variants use consistent status and annotation overrides and refreshed rows retain the launch's theme.
- [x] 4.4 Apply preview chrome roles while preserving captured ANSI content and theme-independent candidate behavior; verify unchanged visible text, IDs, order, column alignment, search matching, and preview/focus targets across representative dark, light, terminal, and custom themes.

## 5. Documentation and integrated acceptance

- [x] 5.1 Update README configuration examples, all action defaults, supported imported fzf actions and precedence, theme names/roles/Herdr-compatible color forms and aliases, diagnostics, direct lookup, and reopen-to-reload behavior; verify valid JSON examples and coverage of every public setting, omitted/null versus empty arrays, required accept/close bindings, hidden disabled-action hints, non-blocking fzf exclusions versus blocking Pickr config errors, the Catppuccin default, and `rose-pine` opt-back.
- [x] 5.2 Update existing fixed-key/color fixtures to use explicit configurations where appropriate and run `lua tests/test.lua`; verify the full suite passes with deterministic fixture settings independent of the author's ambient configuration, including existing refresh, socket, subprocess, ordering, label, and filter checks.
- [x] 5.3 Run the README's isolated relocation regression procedure with only distributed files, a checkout path containing spaces, and an unrelated caller directory; record dependency versions and verify config discovery and new modules require no parent source files or runtime network access.
- [x] 5.4 Perform interactive acceptance across all five variants with default and remapped action keys, inherited navigation/editing, multiple acceptance/refresh aliases, failure/retry, and close/switch during refresh; record actual results and verify hints remain usable at the default popup dimensions.
- [x] 5.5 Visually inspect Catppuccin, a light theme, Rosé Pine, terminal, and role overrides in initial and refreshed lists and previews; record results verifying contrast, annotation consistency, original preview ANSI colors, and reopen-to-adopt configuration edits.

## Acceptance notes

- Final acceptance: after receiving the final inherited-control, visual-theme, and reopen-to-adopt checklist, the user signed off with “Let's mark as done.” Tasks 5.4 and 5.5 are complete on that user sign-off. Earlier pending notes below are historical progress records, superseded by this acceptance.

- Theme/integration verification: `lua tests/test.lua` passed after activating the shared theme roles, including all five variants across Catppuccin, Catppuccin Latte, Rosé Pine, terminal, and custom colors; refresh stability, unchanged visible text/IDs/alignment/order/filter targets, and existing captured-ANSI preview checks. README JSON examples and inventories are checked by `tests/documentation.lua`. The real-fzf PTY action suite also passed after theme integration. Visual acceptance remains pending under task 5.5.
- Relocation verification: `lua tests/relocation.lua` (with an explicit temporary parent) passed from an isolated `Herdr Pickr/` distribution copy and unrelated `caller/` under `env -i`, using only the README-listed distributed files. The copy was removed afterward. Versions: macOS 26.6.2 (25G83), Herdr 0.9.0, Lua 5.5.1, Homebrew luv 1.52.1-0 / libuv 1.52.1, fzf 0.74.3, Git 2.50.1 (Apple Git-155). No parent source files or runtime network access were required.

- Tasks 3.1–3.3 and 2.3: `tests/FZF_COMPATIBILITY.md` records the pinned grammar and supported action inventory. `python3 tests/fzf_compat.py`, `lua tests/test.lua`, and `python3 tests/fzf_actions.py` passed. Real fzf checks cover released Enter navigation, inherited query editing, owner-key precedence, event/mixed-chain and output-option isolation across all five variants, and inherited preview scrolling to the final fixture line. Importer fixtures cover both sources, separators, aliases, quoting/comments/escapes, replacement/append chains, malformed/unreadable sources, exclusions, and launch-snapshot isolation.

- Tasks 2.2/2.4 live acceptance: the user reported the configured-key checks passing, including readable alias hints, remapped accept/close/refresh/preview controls, acceptance gating during refresh, available close/preview/switch controls, and failure/retry preservation. This completes the live portion of the action integration checks below. Task 2.3 remains open for inherited-key restoration.

- Action integration update (tasks 2.2–2.4 remain open): bindings, variant expectations/reverse routing, footer and retry hints, and refresh unbind/rebind lists now use the resolved map. With no variant shortcuts, the owner interprets fzf's output without an expectation prefix. Built-in lifecycle routes are guarded, with Ctrl+D retaining deletion without EOF cancellation. `python3 tests/fzf_actions.py` passed against real fzf for both acceptance aliases and configured closing in all five variants with optional actions disabled. Remapped-route and hint/gating fixtures were added. Live hint layout and remapped refresh/failure/switch acceptance remain pending; inherited-key restoration awaits the importer.

- Task 1.5 final acceptance: the user confirmed oversized cell counts now open successfully after clamping, completing the live sizing checks. Earlier pending notes below describe the implementation history.
- Task 2.1 audit: the existing keymap implementation and passing configuration fixtures cover all nine defaults, aliases/replacement arrays, seven optional empty arrays, required accept/close arrays, invalid keys/events/expressions, and effective-key conflicts. Reviewed normalization against fzf v0.74.3 `src/options.go` (`parseKeyChords`).

- The user confirmed the corrected task 1.5 popup launch and sizing checks pass, then requested clamping cell counts above 65535 instead of blocking launch. Configuration now caps oversized integer counts; interactive acceptance of this revised oversized-count behavior remains pending.

- Task 1.5 live regression: the user reported popup launch failure even with missing or empty configuration. Plugin logs showed `invalid_params: overlay and popup plugin panes target the active pane`. Removed the explicit `workspace_id` target from popup requests; origin scope remains in `PICKR_ORIGIN_WORKSPACE_ID`. Socket fixtures now assert that popup requests omit workspace/pane targets. Live acceptance remains pending.

- Task 1.5 implementation: all action launchers now use `plugin.pane.open` socket requests with popup placement, focus, original workspace, configured dimensions, and the validated settings snapshot. The shared transport validates reply IDs and popup identity and retains timeout/cleanup behavior. `lua tests/test.lua` passed, including dimension validation, launcher fixtures, settings handoff, split replies, errors, malformed responses, EOF, and timeout cleanup. Live default/percentage/cell-count dimensions, Herdr clamping, and reopen behavior remain pending; the task remains unchecked.

- Task 1.4: `lua tests/test.lua` passed, including both launch defaults and simulated toggled-state handoff across all 25 routes. The user subsequently reported all requested live Herdr checks passing: both initial states, toggled visibility across switches, empty/zero-match lists, switches during loading and after failure, refresh/retry preservation, and reopening to the configured default. This confirms the revised session-state behavior, superseding the earlier reset-on-switch acceptance.
