## 1. Configuration and launch snapshot

- [x] 1.1 Add `refresh.interval_ms` resolution and strict validation in `src/pickr/config.lua`, with default zero and integer bounds `0..2147483647`; verify configuration tests cover missing/null/empty/default values, positive boundaries, invalid types/ranges, unknown fields, and independence from preview visibility and `keys.refresh`.
- [x] 1.2 Carry and validate the interval in a consistently versioned resolved settings handoff for action, direct pane, and direct picker launches; verify launch/configuration fixtures retain the original value through intervening config edits, reject incompatible/malformed handoffs, and adopt edits only on reopening.

## 2. Usable manual background refresh and publication

- [x] 2.1 Separate published display/acceptance state from candidate refresh progress in `core.lua` and `runtime.lua`; replace clear-list/unbind behavior with retained results and compact status reporting. Verify session tests cover fetch/preparation failure, timeout, retry, empty retained lists, and acceptance during fetch/failure while preserving entry-restoration readiness tests.
- [x] 2.2 Implement native `--id-nth=1` tracking with `reload-sync` over prepared local rows and register candidate data before replacement. Verify in real fzf that delayed fetches remain interactive, selection follows changed text/order, native fallback/no-match behavior applies, emitted old/pending rows are validated, and subsequent navigation is not overridden. Keep entry restoration separate. Enforce a single publication deadline and verify fatal stalled-publication cleanup. Record the deadline, normal publication latency, tested version, and accepted native ignored-input behavior; no custom buffering or terminal layer is required.
- [x] 2.3 Update transition/result handling to save latest query/displayed selection during refresh or failure and cancel obsolete work, retaining pending entry identity only for unfinished entry restoration. Verify real-fzf type/scope round trips, current-type shortcuts, literal queries, hidden/shown previews, no-match memory, rapid transitions, and late-callback rejection using native fzf input semantics at publication.
- [x] 2.4 Verify complete publication using changing snapshot fixtures for all four types and supported scopes: additions/removals, moved panes, status text/indicator and agent priority, labels/titles/counts/directories, configured columns/theme/header/footer, updated preview targets, stable-ID selection, and deleted/filtered/no-match fallback. Confirm acceptance of a since-closed displayed target reports exact-target failure without substitution.

## 3. Recurrent scheduling and resource ownership

- [x] 3.1 Add the per-view repeating timer after readiness, calling the shared refresh path and skipping busy triggers through publication. Verify no periodic work with interval zero, first tick after one interval, manual/automatic overlap without queued catch-up, and automatic recovery after failure.
- [x] 3.2 Keep list scheduling active with hidden previews, disabled manual refresh keys, empty lists, and zero matches; restart cadence only for fresh view entry and dispose it on acceptance/close/transitions. Verify request-count and lifecycle fixtures cover independent popups, fresh destination cadence, unchanged-effective-scope behavior, prompt shutdown, and no post-close or cross-view work.

## 4. Recurrent selected-preview updates

- [x] 4.1 Trigger a fresh selected visible preview after successful publication even for unchanged rows/targets, following refreshed tab/space remembered focus. Verify real-fzf fixtures show new terminal output without navigation, exact Panes/Agents targets, changed Tabs/Spaces targets, no-pane/unavailable messages, ANSI preservation, and no screen reads when hidden or without a selection.
- [x] 4.2 Coordinate preview helper ownership/start/completion with request and target tokens, coalescing same-target periodic requests and releasing busy state on completion/error/cancellation/timeout while retaining direct one-shot preview invocation. Verify slow captures exceeding the interval can finish, killed helpers do not suppress future updates, obsolete output never appears after navigation/hide/transition/close, and preview failures do not block candidate updates or acceptance.
- [x] 4.3 Use native fzf preview scrolling, accepting a return to the top on same-target refresh, and refresh immediately on showing/changing selection. Verify real-fzf scrolling/reset, hide/show, target changes during capture, and short-content bounds; document the accepted native behavior without custom scroll tracking.

## 5. Documentation and integrated regression checks

- [x] 5.1 Update `README.md` highlights, defaults, interval example/bounds/cadence, background-refresh failure/retry, preview targets, and reopen semantics. Replace fetch-wide clearing/acceptance locks and refresh-start recovery claims with interactive fetching and native fzf replacement, explicitly noting that input can be ignored during the final swap. Keep entry-restoration distinctions and verify `tests/documentation.lua` and usage-text consistency.
- [x] 5.2 Integrate new fixtures and replace old non-selectable refresh expectations. Run `lua tests/test.lua` and confirm configuration, launch, native publication/deadline expiry, scheduling, previews, scopes/keybindings/columns/footer/memory, and cancellation pass. Record publication latency and environment limitations explicitly.
- [x] 5.3 Run `openspec validate live-picker-refresh --strict` and review every delta scenario, confirming opt-in automatic refresh and shared manual/native publication semantics. Verify exact returned-target validation and document native input suspension; record the verification summary before marking implementation complete.
